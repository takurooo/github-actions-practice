name: Action Sample

on:
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: python version
        run: python --version
      - name: echo CONDA
        run: echo $CONDA

